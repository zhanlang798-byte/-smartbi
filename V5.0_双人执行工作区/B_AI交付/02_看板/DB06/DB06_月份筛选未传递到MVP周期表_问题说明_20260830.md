# DB06 月份筛选未传递到 MVP 周期表｜A 核查说明

> 2026-08-31 13:41:45 +08:00 更新：A 已将该 `1:1` 关系的筛选方向改为双向，保存并清缓存成功；三个月份预检均为组件1=1条、组件3=360条。原问题已解除，当前状态为 `READY_B_REVIEW`。详见 `DB06_关系修复待B复核_20260831.md`。

## 现场读数

- 页面：`DB06_XH202612_V50_CYCLE_CONTEXT`
- 月份默认值：`2025-12-31`
- 观察窗口：已恢复为 `1y`
- 组件 1 `周期状态与证据表`：仍为 `660` 条
- 组件 3 `情景与三策略明细`：`360` 条
- 组件 2 保持全量 660 月趋势，属于设计要求
- 截图：`DB06_月份联动异常_C01为660_C03为360_20260830.png`
- 最终全页截图：`DB06_最终页面全景_20260830.png`

## B 侧实机检查

1. 月份筛选器已经位于组件 1 和组件 3 的“影响此页上多个组件的筛选器”区域，并非漏勾作用范围。
2. 同一个月份值能把 `V50_portfolio_scenario` 对应的组件 3 限制到 2025-12，但不能把 `V50_MVP_cycle_state` 对应的组件 1 限制到 2025-12。
3. 冻结关系证据 `00_共享/模型交接/RELATIONSHIP_DETAIL_MVP_CYCLE_V50.png` 显示 `V50_dim_date` 与 `V50_MVP_cycle_state` 已建一对一关系；结合页面实测，日期维到 MVP 表的筛选传递方向或平台生效状态需要由 A 复核。

## A 侧需要核查

- 只读核对关系：`V50_dim_date.month_end = V50_MVP_cycle_state.month_end`。
- 确认基数保持 `1:1`，筛选方向能够从日期维传向 MVP 周期表。
- 不改表数据、不新增影子模型；如需修改冻结关系，先登记模型变更并重跑关系审计。
- 修复后回到 DB06，用月份 `2025-12-31` 验证组件 1 为 1 条、组件 3 为 360 条，并补截图。

## 当前判定

历史判定为 `BLOCKED（A 模型关系核查）`。截至 2026-08-31，该关系硬阻塞已解除，进入 B 独立复核；B 未签收前仍不写 DB06 正式 PASS。

## 修复后必须复测

1. 月份 `2025-12-31`：组件 1 应为 1 条，组件 3 应为 360 条。
2. 月份 `2020-04-30`：组件 1 与组件 3 均须与辅助表对账并截图。
3. 月份 `2023-10-31`：组件 1 与组件 3 均须与辅助表对账并截图。
4. 复测后补冷/热性能、HIST001/HIST018 同屏证据和 A 只读复核，再决定 PASS/FAIL/BLOCKED。
