# A03 CPI运行复测｜2026-09-04

## 本步结论

**已完成平台CPI实际聚合复测，发现MAX错误，尚未修复。** 模型字段持久化为BIGDECIMAL并不能保证底层聚合按数值执行。A03最终验收不得通过，B签署保持不变。

本次使用正式模型 `MDL_XH202612_V50_COUNTRY_RESERVE`（ID `6b5dff57a4093ba3db07d2903905fe40`），不是P0或其他测试模型。原始粒度为 `iso3 + month_end`，源表 `input.v50_country_monthly_risk`。没有关联、过滤、缺失填零或DISTINCT清洗。

| 检查 | 平台原生结果 | 源端预期（同为两位小数） | 结论 |
|---|---:|---:|---|
| SUM | 2,862,057.90 | 2,862,057.90 | 显示精度一致 |
| AVG | 375.80 | 375.80 | 显示精度一致 |
| MAX | 993.56 | 47,954.24 | 不一致，必须修复 |

跨国CPI均值这里只是类型回归检查，不解释为经济指标。本次没有取得平台SUM/AVG全精度输出，不声称全精度完全相等。

## 根因证据与影响

1. 重新登录后新建临时仪表盘，明确选择正式模型，添加原生指标卡。CPI在维度区，通过原生“复制转度量”创建临时自定义度量；属性显示来源 `维度\V50_country_monthly_risk\cpi_index`、名称 `cpi_index_private`。未修改或保存正式模型。
2. 分别选择合计、平均值、最大值，并重新勾选以应用新的默认汇总依据。配置和结果均保存在对应DOM中；预览截图作为辅助证据。
3. 原生指标最大值993.56，与源文件**文本序最大值**993.558523173606完全对应（IRN，2021-06-30）。真正数值最大值为47954.2419061244（SDN，2023-02-28）。
4. 同一平台底表的只读SQL同时返回：total_rows=7680、non_null_rows=7616、null_rows=64、raw_max=993.558523173606；`MAX(CAST(NULLIF(TRIM(cpi_index),'') AS DECIMAL(30,12)))` 返回47,954.24，转换后AVG返回375.80。没有UPDATE、ALTER、重导入或源工作簿改写。
5. 结合已有底层视图STRING证据及上述同表对照，可确认本路径在聚合前仍采用文本类型。风险为CPI极值/排序结果错误；严重程度高，根因置信度高。不能从此推断所有趋势图或全部59字段都错误。
6. 之前“字段不可选”属于自动交互所见状态，不是平台权限不足的结论。字段原生checkbox可选择；目录父节点的aria-disabled标记影响了部分自动选择。没有修改DOM状态或绕过权限控制。

## 已保存证据

- `01_SUM_PLATFORM.txt`、`02_AVG_PLATFORM.txt`、`04_MAX_PLATFORM.txt`：正式模型绑定、无维度指标卡、汇总方式及实际显示值。
- `03_AVG_PREVIEW.*`、`05_MAX_PREVIEW.*`：原生预览。窄浏览器视口使卡片较小，完整数值和配置以DOM为主。
- `06_SQL_TYPE_COMPARISON.*`：平台底表文本/数值同查询对照。SQL预览共1行，是全表聚合，不是只聚合预览样本。
- `CPI_TYPE_COMPARISON.sql`：上述只读查询，格式化存档，与界面SQL语义相同。
- `SOURCE_COMPARISON.json`、`check_source.py`、`CPI_RECHECK.ipynb`：源端全表重算与文本序最大值核对。
- `LEDGER_CHANGELOG.json`、`LEDGER_BEFORE.png`、`LEDGER_AFTER.png`：类型审计表只更新F88备注，保留383 PASS/59 FAIL和全部签署。

源文件SHA-256：`0d83e648b5e142661e4f3726a164887c5c8bbf25c24d5e04e6a1449265879b0e`。未公开原始迁移XML、连接元数据、凭据或首页账号信息。

## 下一步A按顺序执行

1. 在变更前取得当前正式模型和目标源表的可恢复备份，确认引用该字段的组件/指标范围。
2. 依据既有8月28日裁决，修复底层CPI数值类型或源查询显式数值化，保留原表名/字段引用及64个NULL。不能只改界面显示类型，也不能用某个图表的局部CAST代替正式修复。
3. 保存并重新打开，清理相应缓存后，复测原生AVG/MAX及7680/7616/64边界；MAX应为47,954.24。核对受影响趋势/同比，并重新检查本表主键，防止重导入改变数据。
4. 生成新受控导出及分层元数据/哈希对照后交B独立复核，再处理资源锁和最终冻结。

当前状态：`FAIL_MAX_PENDING_REPAIR`。该缺陷阻止CPI最终指标验收及相关最终冻结，不撤销既有G2启动放行或G3六页初验。G4—G6未签PASS，AI六题继续按用户要求跳过。
