# A03 类型分层与持久化核验（2026-09-04）

## 本批结果

第三步已完成“类型持久化”部分，**尚未完成计算对账闭环，不是 A03 最终通过**。

- 对旧台账的59项异常逐项匹配：源表对象名 → 源字段ID → 同一视图的模型字段，不靠重复别名猜测。
- 9月1日已有导出中，59项模型字段类型均符合对应类型要求；9月4日重新加载正式模型后，9张相关表的在线字段类型与该导出全部一致。
- 模型层分布：长浮点型 BIGDECIMAL 1项、浮点型 DOUBLE 37项、日期 DATE 10项、整型 INTEGER 11项。
- 同一导出的底表视图层仍有57项 STRING，另2项已是 DOUBLE/INTEGER。因此“旧台账59 FAIL”不能直接解释为“当前模型59项都未保存成功”。
- `country_monthly_risk.cpi_index` 的底表视图是 STRING，模型字段是 BIGDECIMAL，重新加载后仍显示长浮点型。没有再次改型或保存正式模型。

## 证据范围

| 层次 | 证据 | 本批可确认内容 |
|---|---|---|
| 历史台账 | 提交 `6c49f46` 中的类型台账与第一步 preflight | 历史底表口径383 PASS / 59 FAIL |
| 已持久化模型 | 本机受控 `migrate_MODEL_METADATA_PATCH_20260901.xml` | `AUGMENTED_DATASET/fields/field` 中59项类型；不是本批新导出 |
| 当前模型 | `01_MODEL_BEFORE_REOPEN.txt`、`02_MODEL_AFTER_REOPEN.txt`、`LIVE_TYPE_TABLES.json`、`LIVE_V50_*.txt` | 重新加载后CPI和9张表的在线类型，59项逐一匹配 |
| 有限数据预览 | `03_MODEL_CPI_PREVIEW.txt` | CPI能返回有限预览值，不能证明全表聚合或计算正确 |
| 计算尝试 | `04_ADHOC_DISABLED_FIELDS.txt`、`05_PIVOT_DISABLED_FIELDS.txt`、`06_DASHBOARD_DISABLED_FIELDS.txt` | 临时新建的三个分析入口字段树均呈不可选状态，未取得聚合结果 |
| 源端全表基准 | `CPI_SOURCE_BASELINE.json` | 仅源工作簿的CPI全表计算结果，不是平台结果 |

模型资源ID：`6b5dff57a4093ba3db07d2903905fe40`。指定模型：`MDL_XH202612_V50_COUNTRY_RESERVE`。

已有导出的模型最后修改时间：2026-09-01 19:46:31。原始XML SHA-256：`fa99dddab3ab7146104a77ac5af06936c317953f93c253643378e10a5bd8314f`。原始XML可能含内部连接信息，只保留在本机受控目录；本批提交白名单元数据，不提交原始XML。

`TYPE_LAYER_RECONCILIATION.json` 和同名CSV保留全部59条匹配结果、类型层次、哈希、计算待验收与B待复核状态。类型相符不证明日期/三态的值域正确、聚合正确、独立恢复通过或最终冻结。

## CPI计算基准与未完成项

源文件：`country_monthly_risk.xlsx`，SHA-256 `0d83e648b5e142661e4f3726a164887c5c8bbf25c24d5e04e6a1449265879b0e`。

| 指标 | 源端全表结果 | 平台全表结果 |
|---|---:|---|
| 总行数 | 7,680 | 本批未新增查询；行数和主键已有第二步证据 |
| CPI非空数 | 7,616 | 待测 |
| CPI空值数 | 64 | 待测 |
| CPI平均值 | 375.7954180183989487788865546 | 待测 |
| CPI最大值 | 47954.2419061244 | 待测 |

计算仅忽略空单元格，不填0、不去重。跨国CPI平均值仅作数据类型/计算诊断，不作为经济指标解释。

按8月28日G2启动裁决，下一步先在正式模型的可用分析入口取得平均值、最大值或同比的实际结果并与源表对账。当前“字段不可选”的原因尚未定位，不能断言是权限不足，也不能断言CPI计算失败。取得实际计算结果后再决定是否需要改底表；不要基于旧59 FAIL再次批量改型。

## 台账更新与校验

- 权威 `SMARTBI_TYPE_AUDIT_V50.xlsx` 只更新59个异常字段的F列备注，记录底表/模型/在线类型和未完成边界。A—E列原值、原判定及其他单元格保持不变。
- 为容纳新备注，仅这59行自动调整行高。未添加工作表、列、公式、签署或B复核。
- 原备注与新备注完整保存在 `TYPE_NOTES_CHANGELOG.json`。前后渲染为 `TYPE_NOTES_BEFORE.png`、`TYPE_NOTES_AFTER.png`。
- `verify_type_notes.py` 相对不可变提交 `6c49f46` 核验：恰好59个备注值变化，验收列与所有单元格样式未改；包结构未改。序列化仅另有无语义变化的关系ID更新，引用目标已核验。
- 现行台账判定计数仍为383 PASS / 59 FAIL。该计数保留历史底表口径，须结合本批分层记录阅读，不能充当当前模型改型待办数量。

## 复现

用已配置的Python运行 `verify_type_layers.py`，只读原始源表、受控XML与在线证据，生成白名单JSON/CSV；用 `verify_type_notes.py` 检查权威台账相对基线的窄范围变化。脚本不修改源表或XML。

台账作者脚本 `update_type_notes.mjs` 仅在基线哈希符合时允许 `--apply`，在已经回写的台账上会拒绝执行，避免反复覆盖。`outputs/`、`backups/` 和 `node_modules/` 不进入版本库。

本批临时查询、透视和仪表盘均未保存并已关闭；打开分析时模型提示保存，均选择“不保存”。正式模型未新增表、未保存或发布。未重开跳过的AI六题，未操作恢复租户，未变更G4—G6或B签署。
