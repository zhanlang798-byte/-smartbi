# 待平台维护方确认的原位 CPI 修复入口（尚未发送）

对象：模型 `MDL_XH202612_V50_COUNTRY_RESERVE` / `6b5dff57a4093ba3db07d2903905fe40`；源表 `input.v50_country_monthly_risk`；字段 `cpi_index`。

问题：模型覆盖类型BIGDECIMAL已持久化，但源字段STRING仍令原生MAX按文本返回993.56，数值预期47,954.24。仅修改数据连接目录类型不生效，已回退；原表右键“编辑”禁用，不能直接编辑为SQL。无法据此判断账号权限原因。

可重复证据：本目录三个只读SQL已实跑。完整18列CAST查询返回7680行、7616非空、64NULL、0空白、复合主键无重复；CPI MAX47,954.24、AVG375.80、SUM2,862,057.90。实际查询文本及原生结果均在本包。

请确认一种受支持的实施入口及回退办法：

1. **优先原位修复**：在现有源物理表对CPI做数值化，保持同一表名/列名、18列结构、iso3+month_end粒度、精度和NULL语义；提供平台入口或由源表维护方执行并返还结果。不是仅改目录元数据。
2. **如果只能更换查询**：提供保留原视图及18字段引用ID的SQL查询替换/迁移流程。在正式操作前，用当前模型和最新页面备份验证2条关系、CPI模型字段、DB02私有度量、筛选器及其他引用。不可仅凭同名列删除重建旧表。

本请求不授权更改其他表、删除数据、公开连接信息、切换MPP抽取模式或修改已签收页面逻辑。

验收与回退：正式模型原生聚合及DB02趋势/筛选通过、新XML引用比较通过后才交B复核；若引用丢失、行数/NULL改变或原生MAX仍错，恢复正式变更前备份。本包没有执行上述正式操作，也不是B签收单。
