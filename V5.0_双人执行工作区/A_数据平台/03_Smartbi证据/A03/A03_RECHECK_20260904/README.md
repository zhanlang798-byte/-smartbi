# A03 第一阶段：台账一致性修复｜2026-09-04

> 后续进展：同日14:04已取得21表完整平台主键聚合结果，见 `../A03_PK_AUDIT_20260904/README.md`。本文保留第一阶段当时的观察和待办，不倒改历史快照。

## 本次结果

本批只完成 **A03 台账一致性修复**，不代表 A03 最终验收通过。

1. 权威工作簿 `../SMARTBI_TYPE_AUDIT_V50.xlsx` 已从 379 PASS / 63 FAIL 修正为 **383 PASS / 59 FAIL**，共 442 项。
2. 仅回写已有证据放行的 4 个布尔字段：`country_monthly_risk.is_proxy`、`country_monthly_risk.is_imputed`、`country_event.known_at_decision_time`、`global_cycle_month.vintage_reconstructed`。
3. 这些字段的期望类型仍为“布尔”、记录的平台类型仍为“字符串”；PASS 表示既有平台限制例外放行，**不表示改型成功或新 XML 已验证**。
4. 只改 E/F 列第 96、97、136、179 行的判定及备注，共 8 个值；仅对应 4 行自动调整行高。其他值保持不变，导出后重新导入复核通过，前后局部渲染图已保存。
5. `../a03_prepare.py` 已退役并默认拒绝运行，旧前缀已对齐为 `V50_`；避免初建草稿覆盖后续人工证据。
6. 新只读核验入口 `a03_preflight.mjs` 检查现有 3 本工作簿，只输出复核报告，不回写工作簿。

## 依据和复现

- 放行依据：`../A03_EXECUTION_LOG_V50.txt` 中 2026-08-26“平台布尔类型限制证据”及“第二次 XML 导出复核”。
- 截图：`../字段类型截图/A03_平台无布尔类型_20260826.png`。
- 截图 SHA-256：`75ce129572ac215881c1a8278519629fe00b96c515ae40a5b2480b80015abddf`。
- 修订后权威工作簿 SHA-256：`9a8b1d301f77429d1660b6894dc23b4929e8a5aef9ed507b761beb29473acb8f`。
- `A03_WORKBOOK_BASELINE_20260904.json`：修改前台账快照，379/63。
- `A03_WORKBOOK_PREFLIGHT_20260904.json`：修改后台账快照，383/59；**不是平台实测数据**。
- `A03_TYPE_WAIVER_CHANGELOG_20260904.json`：8 单元格变更、原备注及证据哈希。
- `TYPE_BEFORE_*.png` / `TYPE_AFTER_*.png`：3 组局部渲染对照。
- `verify_workbook_diff.py`：独立只读解析 XLSX，与修复前提交 `4f58689` 比较；确认仅 8 个值变化、其他值及 ZIP 部件清单不变。运行结果 PASS，不是平台验收 PASS。
- `LIVE_MODEL_DATE_VIEW_20260904.png`：本次正式模型日期表页面视图，仅证明观察到当前页面，不证明类型或主键完整验收。
- `a03_type_waiver.mjs`：本次窄范围作者脚本；只有显式 `--apply` 且原始 4 项均为 FAIL 才输出候选文件，不直接覆盖权威工作簿。在已修订工作簿上再次 `--apply` 会拒绝执行。

使用提供 `@oai/artifact-tool` 的 Node.js 环境，在本目录运行 `node a03_preflight.mjs`。该命令会刷新同目录报告时间；不得把其成功运行解释为 Smartbi 平台主键检查通过。`node_modules`、候选工作簿和工具中间文件不进入版本库。

旧生成器默认运行应得到 `ARCHIVED_GENERATOR_BLOCKED` 并退出，不能用于日常恢复现行审计工作簿。`A03_ALLOW_ARCHIVE_REBUILD=1` 仅供明确的历史草稿重建，本次没有启用。

## 本次平台只读观察

- 已登录 Smartbi，并定位正式模型 `MDL_XH202612_V50_COUNTRY_RESERVE`（资源 ID `6b5dff57a4093ba3db07d2903905fe40`）。未保存、发布或修改正式模型。
- 模型页面列出 21 个目标对象及额外 `V50_monthly_risk`；额外表不并入 21 表验收范围。
- 正式上传源表位于“数据连接 → 可导入数据库 → input → 数据采集空间 → 当前用户目录”，不是早期 P0 表。
- `V50_dim_date` 的源表属性显示物理表名 `dim_date_dpkgi0c3510fdb4327001`；`date_key` 为 INTEGER，`month_end` 为 DATE。
- 源表“数据快查/数据预览”页面提示“预览区域只展示前20行数据”。本次未取得全表聚合或全量导出，因此**没有新增平台总行数、空主键或重复主键的通过记录**。
- 当前资源存在、当前字段显示均不能追溯证明“上传前无同名”或替代持久化 XML 证据。

上述是 UI 观察记录及一张有限页面视图，不是新的平台验收截图包。未提交包含账号信息的原始 DOM；未上传原始 XML、连接地址或数据库凭证。

## 下一步顺序与完成条件

| 顺序 | A 的下一步 | 完成条件 | 当前状态 |
|---|---|---|---|
| 1 | 台账一致性修复和旧脚本退役 | 4 项既有例外回写，383/59 一致，保留原证据 | 本批完成，待 B 检查本批变更 |
| 2 | 21 表平台全表主键核验 | 逐表取得总行数、含空键行数、重复键组数及多余行数，保存对象/时间/结果 | 未取得，不能用截断预览替代 |
| 3 | 类型持久化单字段验证 | 对指定正式表验证保存后类型、运行聚合与源端一致及可追溯导出；成功后再处理其余字段 | 59 项仍待核，不批量盲改 |
| 4 | 资源锁历史证据或风险处置 | 既有审计证据，或明确记录不可追溯并由有权人接受风险 | 不倒填“上传前无同名” |
| 5 | A04 与最终候选包一致性 | 冻结版本/时间、关系 #14 的 40 行未匹配口径、最新依赖导出一致 | 后续处理，不改现行 G2 启动放行 |
| 6 | 独立恢复与交 B 复核 | 干净目标恢复原始受控包，记录独立验证，再分别完成 A/B 签收 | 缺干净目标；本批不操作当前非空租户 |

本次不重开已按用户指示跳过的 AI 六题；正式路线仍为内置“数据洞察”+指定模型，成绩保持 19/25。未代签 B，未上调 G4—G6。
