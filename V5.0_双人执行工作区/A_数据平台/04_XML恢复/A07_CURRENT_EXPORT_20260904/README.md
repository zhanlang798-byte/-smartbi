# A07 当前六页及模型原生导出核验

执行日期：2026-09-04；下载时间：17:33:13 +08:00。

## 结论

`NATIVE_PACKAGE_REFRESHED_STATIC_AUDIT_PASS / READY_FOR_B_PACKAGE_REVIEW`

最新版完整包已取得，解除 `PACKAGE_REFRESH_REQUIRED`。这仅证明本次导出包的完整性、版本及静态配置；独立恢复仍为 `CLEAN_TARGET_BLOCKED / RECOVERY_NOT_STARTED`，A07整体及G4—G6不因此通过。

## 原生过程及原件

1. Smartbi主页恢复后，在“运维设置 → 导出资源”的分析报表树按ID勾选DB01—DB06，仅选本项目六页。
2. “添加依赖资源”中保留正式模型及页间引用，取消共用关系数据源对象；资源权限和行权限未勾选。未更改平台权限。
3. 点击原生“确定”，浏览器报告下载事件；用户确认已保存，下载目录取得 `migrate (12).xml`。本次没有手工拼接或编辑XML。
4. 按原字节复制为本地受控文件 `../A07_PAGES_DB01_DB06_WITH_MODEL_20260904.xml`，保留下载原件和8月31日历史两包。

| 项目 | 值 |
|---|---|
| 文件字节数 | 1,036,630 |
| SHA-256 | `C5B2CFBF2BCBEF8AA277A1D0A5AB6F94DFF07BEF27168D00205D054D137BA51D` |
| 顶层内容 | SMARTBIX_PAGE×6、AUGMENTED_DATASET×1、CUSTOM_ICONS×1 |
| 正式模型 | MDL_XH202612_V50_COUNTRY_RESERVE |
| 模型ID | `6b5dff57a4093ba3db07d2903905fe40` |
| 模型最后修改时间 | 2026-09-02 23:44:40 |
| 模型规模 | 22视图、460发布字段、19关系（SINGLE×18，BOTH×1） |

## 与已有证据的版本核对

完整模型的规范化XML SHA-256为 `c19533c42b1fa5376c5790aeffe0eb9ea9366299695dd17ef5026ca46693a7ea`，与9月4日持有的模型备份完全一致（不仅是22视图/19关系的计数一致）。引用旧CPI备份只为配置比较，没有修复或复测CPI。

| 页面 | 新包lastModified | 相对8月31日旧包 |
|---|---|---|
| DB01 | 2026-08-27 23:10:44 | 页面完整子树一致 |
| DB02 | 2026-08-29 20:45:33 | 页面完整子树一致 |
| DB03 | 2026-09-01 21:57:39 | define配置已变化，较新版本已收录 |
| DB04 | 2026-09-02 00:19:36 | define配置已变化，较新版本已收录 |
| DB05 | 2026-09-01 22:27:01 | define配置已变化，较新版本已收录 |
| DB06 | 2026-08-30 15:55:18 | 页面完整子树一致；模型中双向关系仍在 |

六个页面ID均与锁定清单一致，均引用同一正式模型。DB01定义含DB02目标ID，DB02定义含DB04目标ID。DB04定义含期限/策略/窗口截止日/周期状态/黄金权重、scenario_id、七个grid_g0.xxx档位、weight_sum及全量说明。这里只作静态标记/引用检查，不替代目标环境中的跳转、联动或数据复测。

## 安全与交付

- 关键词扫描中，密码、令牌/Bearer/票据、Secret/API Key、Cookie、私钥头均为0；这是有明示模式的静态扫描，不宣称穷尽所有秘密形式。
- 原件含22处JDBC引用和22处数据库账户 `user` 键。无独立DATASOURCE对象不代表没有连接元数据。
- **原始XML仅限受控内部交付，禁止上传公开GitHub。** 原件已被现有.gitignore规则排除；公开仓库只记录哈希、对象元数据、脚本和本报告。
- XML是资源配置包，不是21张源数据表或运行环境的全量备份；目标环境需单独准备同构数据、数据源映射及凭据。

## 下一步与签署边界

1. B可立即独立核对新包哈希、六页ID、模型版本、DB04静态标记及安全限制；原件通过受控渠道提供。
2. 独立干净目标尚未提供。管理员须先确认目标、版本兼容性、源数据与映射，记录导入前无项目对象，然后A执行一次原生导入。
3. 依恢复操作清单核对对象、KPI、六页筛选及跳转、内置数据洞察选择指定模型，保存日志，再交B本人复核。
4. 最终模型冻结、B签收和G6保持未完成。AI六题继续跳过，19/25；CPI停止修复，既有MAX失败继续披露。

执行记录：A（用户授权，Codex协助导出和代录本轮事实）；仅本轮导出及静态核验通过。不代签B，不补签历史同名，不声明恢复成功。

复核文件：`EXPORT_AUDIT.json`、`audit_export.py`。脚本只读取受控原件，验证固定输入哈希，输出不含连接地址/数据库账户值的元数据；重跑需要本地原件，不从公开仓库下载原XML。
