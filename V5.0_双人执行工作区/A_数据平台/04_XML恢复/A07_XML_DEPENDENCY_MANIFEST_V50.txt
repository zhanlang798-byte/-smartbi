A07 XML 依赖清单 V5.0
更新时间：2026-09-04（版本一致性补记）

现行限制：8月31日两个包仅为历史分包，与9月4日所持模型快照有18项字段属性差异，必须先取得最新版完整六页+同一模型分包；以下对象清单不等于当前完整版本已导出。

一、正式恢复依赖
1. 主模型 MDL_XH202612_V50_COUNTRY_RESERVE。
2. 22 个 BASIC_TABLE 视图（18 张正式表及共享维度/辅助对象，以模型基线 XML 实际内容为准）。
3. DB01—DB06 六个页面及页面跳转依赖。
4. 目标环境同构关系数据源映射；凭据必须在目标环境单独配置，不进入恢复包。
5. 平台内置“数据洞察”能力。该能力是 Smartbi 平台提供的正式 Agent，不是本项目自定义迁移对象。

二、已落盘历史页面分包（待原生刷新）
1. A07_PAGES_DB01_DB02_DB04_WITH_MODEL_20260831.xml：DB01、DB02、DB04、主模型及22个BASIC_TABLE，模型关系 SINGLE×18、BOTH×1。
2. A07_PAGES_DB03_DB05_DB06_WITH_MODEL_20260831.xml：DB03、DB05、DB06、主模型及22个BASIC_TABLE，包含DB06日期维与周期状态双向修复。

三、补充历史诊断资产
- AIP_XH202612_V50_DECISION：已定位，未取得可下载独立 XML；不属于正式评分恢复依赖。
- KB_XH202612_V50_GOVERNANCE：已定位并完成 45 条知识的原生 CSV 备份；不属于正式评分恢复依赖。
- AGENT_XH202612_V50_ASSISTANT：已定位，编辑页显示未发布，未取得可下载独立 XML；只保留历史诊断用途，不属于正式评分恢复依赖。
- IM：按当前项目范围豁免/不适用，最终清单保留豁免说明。

四、建议恢复顺序
1. 在独立干净目标配置同构数据源映射。
2. 导入共享模型及表视图。
3. 导入 DB01—DB06 页面并复核跳转。
4. 验证内置“数据洞察”可选择指定模型。
5. 复核 18+3 表、六页及 130/40/23/20/734 KPI。
6. 如需保留历史诊断资产，再在非正式副本中恢复 KB CSV，并单独记录 AIP/Agent 人工重建，不影响正式验收。

状态：历史六页+主模型 XML 哈希有效；最新版PACKAGE_REFRESH_REQUIRED；KB补充备份COMPLETE；独立干净目标恢复及B独立复核待完成。
