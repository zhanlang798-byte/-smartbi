# 交B复核前技术预检

执行时间见`TECHNICAL_PRECHECK.json`。本轮只检查本地已归档证据及文件绑定，不查询Smartbi、不读取Excel单元格、不修改历史报告或签署。

交付入口：[A→B当前证据复核单](../../../00_共享/每日协调/A_TO_B_REVIEW_WORKLIST_20260904.md)。

## 已完成与限制

1. 使用已审查的`../A07_CURRENT_EXPORT_20260904/audit_export.py`，将输出重定向至系统临时目录，全部断言通过，结果与原`EXPORT_AUDIT.json`完全相同；原件和旧报告不改。检查配置包、固定模型快照和两份历史分包的原始SHA-256，不对XML做文本归一化。
2. 直接读取已捕获的主键JSON、SQL和DOM，复算21行完整性、逐行回指、总数与异常数。不运行会重写旧报告的`verify_pk_evidence.mjs`，也不把本地结果冒充B新执行的平台SQL。
3. 对账工作簿只校验已登记哈希，类型工作簿只校验备注变更链和最终文件哈希；4个布尔例外、59个类型备注及最后F88备注均取自原JSON。没有重新逐格审核工作簿，因此B单元格审核仍待完成。
4. 风险说明的源工作簿和引用文档哈希全部复核一致；没有重新进行CPI计算或AI复测。
5. 性能旧清单中4项与本机原始字节哈希不符。初次严格比对未通过后，另行读Git提交中的原始blob：四项均与旧哈希一致。本机仅把CRLF转LF后即逐字节等于Git版本，内容等价性已证。报告并列旧哈希、本机原始哈希、Git blob哈希和LF规范化哈希，`rawByteHashMatches=false`保留，不写成原始哈希相同。此例外只用于这4份已证换行差异的文本，绝不扩展至XML或Excel。
6. 旧性能状态文本的24次记录最大9.458秒，均≤10秒；仅检查记录，不证明今天平台耗时。

## 复现

在有原仓库、受控XML原件、Git和Python的本机运行：

```powershell
python precheck.py
```

脚本按自身路径定位仓库，重跑仅刷新本目录`TECHNICAL_PRECHECK.json`的时间/复核基线与结果。引用输入的原始哈希在执行前后核对，不覆盖任何工作簿、原XML、旧审计JSON或性能文本。首次运行环境使用Codex捆绑Python，不需额外包。

结果中`inputSha256`是本轮本机原始字节清单；公开Git检出文本可能有换行差异，须按上文分别核验原始字节与文本内容。它不是全仓库冻结清单或签名。

## 交付与签署

公开仓库仅保存本说明、预检脚本与白名单结果，受控原XML不上传。脚本不会输出连接地址或账户值。

状态：`A_TECHNICAL_PRECHECK_COMPLETE_TEXT_EOL_DISCLOSED_B_REVIEW_PENDING`。B独立复核/签署、有权人风险接受、独立恢复、最终冻结均未完成；用户跳过的AI和停止的CPI保持原决定。本轮没有修改模型、页面、源数据或权限。
