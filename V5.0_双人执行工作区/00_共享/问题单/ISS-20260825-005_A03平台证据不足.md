# ISS-20260825-005｜A03 平台证据不足，B 无法独立签收

- 提交时间：2026-08-25 21:26 +08:00
- 提交人：执行者 B（AI 协助只读核对；B 本人已确认）
- 对象所有者：执行者 A
- 截止时间：建议 2026-08-26 09:00 早会前
- 状态：BLOCKED

## 对象名

- A03 全量导入 18 张正式表 + 3 张辅助表
- `SMARTBI_IMPORT_RECONCILIATION_V50.xlsx`
- `SMARTBI_RESOURCE_LOCK_V50.xlsx`
- `SMARTBI_TYPE_AUDIT_V50.xlsx`

## 现象

1. A03 日志将状态写为 PASS，但明确注明结论基于 A 报告读数，逐表截图待 B 抽查。
2. A03 目录仅有一张 220×231 截图，画面只显示 6 张无 `V50_` 前缀的 `dim_*` 维表名称，无法证明 18+3 对象行数、主键和类型。
3. 对账表正式 18 表的平台读数使用“`A报告`”文本，未附逐表证据路径。
4. 资源锁 21 项同名检查全部为空。
5. 类型审计 442 个字段的平台类型与判定全部为空。
6. `a03_prepare.py` 仍硬编码旧前缀 `TB_XH202612_V50_`，不能直接复现现有 `V50_` 对账表与资源锁。

## 已确认内容

- 源端 18 表合计 313,593 行，逐表哈希匹配、主键重复 0/空 0。
- 三张辅助表源端为 40/20/660 行。
- XLSX 被拒导后使用的 `MVP_company_data_status.csv` 原始字节带 UTF-8 BOM；去除 BOM 后与原 XLSX 内容逐单元格一致。
- A04 后续截图证明部分对象已进入模型，但不能替代 A03 逐表平台验收。

## 期望结果

1. 提供 18+3 平台对象清单、真实行数和时间证据。
2. 将对账表平台行数、主键重复、空主键填为真实数值并挂证据路径。
3. 补齐 21 项同名检查，证明未覆盖旧对象。
4. 补齐平台类型证据；优先闭环日期、主键、5 个高风险字段及替代 CSV 的首字段名/类型。
5. 统一准备脚本、对账表和资源锁的前缀，或书面确认旧脚本不再作为复现入口。
6. B 复核通过后再关闭本单；不得由 B 直接修改 A 区证据。

## 临时处置

- A03 记为“执行者 A 报告 PASS / B 复核 BLOCKED”。
- G2 不升级，B00 不签收。
- B 不修改 A03 文件，不用口头说明替代平台证据。

## 2026-08-25 B 确认

- A/B 使用项目同一 Smartbi 账号，这是既定运行前提；本问题单不以独立账号不可写证明作为关闭条件。
- 本单维持 `BLOCKED` 的原因仅为 A03 平台行数、主键、同名资源、字段类型与可追溯时间证据不足。
- B 确认：✅（2026-08-25 23:19 +08:00，本人审核无异议）。

## 2026-08-27 B 独立复审

- 21 张平台预览截图已覆盖 18+3 对象，行数证据本项 PASS：18 张正式表合计 313,593，三张辅助表 40/20/660。
- 主键平台查询仍未提供：对账表 21/21 仍写“源端审计+A报告；平台未查询”。
- 资源锁仍未闭环：21/21 同名检查均为“无法追溯”。
- 当前类型审计工作簿实算为 PASS 379、FAIL 63；日志/问题单所述 PASS 383、FAIL 59 尚未回写为一致的权威版本，且 `cpi_index` 页面类型与 XML 仍冲突。
- `A03/a03_prepare.py` 仍使用旧前缀 `TB_XH202612_V50_`，尚无统一的有效复现入口或退役记录。
- 当前结论：本单继续 `BLOCKED`。完整证据矩阵见 `00_共享/每日协调/B_REVIEW_A03_A04_20260827.md`。

## 启动裁决更新（2026-08-28）

- `G2 = PASS（启动放行）`，依据：`00_共享/每日协调/G2_START_RELEASE_20260828.md`。
- 本单保持 `OPEN`，但转为 `NON-BLOCKING`，不再阻塞 B 正式搭建 DB03/DB05/DB06 和配置 AIChat。
- 平台全表主键、资源锁和可追溯时间证据仍须在恢复演练或 G6 最终放行前补齐；本更新不把缺失证据写成已完成。

## A 侧部分修复（2026-09-04，待 B 复核）

- 已依据既有无布尔型证据将 4 项例外回写权威工作簿，实算由 379 PASS/63 FAIL 统一为 383 PASS/59 FAIL；仅 8 个单元格值变更，不改变期望/实际类型。
- 旧 `a03_prepare.py` 已默认禁止执行覆盖式草稿重建，前缀对齐 `V50_`；新增只读台账核验入口。
- 修复说明、前后快照、渲染和逐单元格变更记录：`A_数据平台/03_Smartbi证据/A03/A03_RECHECK_20260904/`。
- 当前平台源表预览有截断提示，本批未取得全表主键查询；资源锁与59项类型问题仍未闭环。
- 本单继续 OPEN/NON-BLOCKING（相对 G2 启动）；G6 最终签收条件不变。未修改 B 历史复核记录或代签 B。
