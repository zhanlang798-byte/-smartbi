# ISS-20260826-005｜A03字段类型保存与XML不一致

## 对象名

- 共享模型：`MDL_XH202612_V50_COUNTRY_RESERVE`
- 目标表：`V50_country_monthly_risk`
- 目标字段：`cpi_index`
- Smartbi模型资源ID：`6b5dff57a4093ba3db07d2903905fe40`

## 现象

1. 执行者A在“表字段属性”中将`cpi_index`设为“长浮点型”。
2. 页面截图能同时识别目标表前缀`V50_country_monthl…`、字段`cpi_index`和类型“长浮点型”。
3. 保存后导出的XML仍记录：`valueType=STRING`、`dataFormat=<字符串-默认值>`。
4. XML中`V50_country_monthly_risk`仅出现一次，`cpi_index`字段也仅出现一次；不存在新旧视图重复导致AI读错的情况。
5. 最新XML模型最后修改时间为`2026-08-26 00:44:04`，说明模型发生过保存，但目标字段类型未进入导出结果。
6. 最新442字段复核读数：PASS 383、FAIL 59、BLOCKED 0；相对初次XML，字段类型变化数0。

## 截图

- `A_数据平台/03_Smartbi证据/A03/字段类型截图/A03_V50_country_monthly_risk_cpi_index_长浮点型_20260826.png`
- 截图SHA-256：`4d6d4dcdb3b5904c25065f41b530c98eb150a88694e835a21d1755c60c32e966`

## XML证据

- `A_数据平台/03_Smartbi证据/A03/XML元数据/A03_METADATA_REEXPORT_V50_20260826_004.xml`
- XML SHA-256：`d2c0acadd9c69137881e97a632927b97827bc2b73a25dc453d62691925d7c5bd`

## 期望结果

1. 刷新并重新打开模型后，页面仍显示`cpi_index=长浮点型`。
2. 同一模型重新导出的XML应显示`V50_country_monthly_risk.cpi_index.valueType=DOUBLE`。
3. 不修改只读源工作簿，不把缺失填0，不用字符串计算或DISTINCT掩盖类型问题。

## 请求检查

1. 请Smartbi管理员确认当前账号是否只有界面改型权限、没有持久化字段类型权限。
2. 请检查模型保存是否需要额外“提交/发布”动作。
3. 请检查XML迁移导出是否读取缓存或原始数据源类型，而非模型字段覆盖类型。
4. 如平台设计上不把字段覆盖类型写入XML，请提供书面说明及可复核的替代导出证据。

## 截止时间

本问题不再阻塞 G2 启动放行；在 CPI 最终指标验收、XML 恢复复核或 G6 放行前完成计算对数并回签。

## 当前状态

`OPEN / FAIL_MAX_PENDING_REPAIR（2026-09-04实际聚合复测失败；保留G2启动放行，不通过CPI最终验收）`

## 追加复核（2026-08-26 00:49）

1. 新导出文件再次核对，归档为`A03_METADATA_REEXPORT_V50_20260826_005.xml`。
2. SHA-256：`021a990bc90e269d535d3bb269f835c8afde565fb67966bc0d3fe5f95ac91a7d`。
3. 模型最后修改时间仍为`2026-08-26 00:44:04`。
4. `V50_country_monthly_risk.cpi_index`仍为`STRING`。
5. 442个目标字段相对初次XML的类型变化数仍为0。
6. 问题可重复，停止要求执行者A反复改型，等待管理员处理。

## 启动裁决更新（2026-08-28）

1. Smartbi 实机再次确认：正式底表 `cpi_index=STRING`，正式模型运行时为 `BIGDECIMAL`；模型数据预览可正常返回数值。
2. 根据 `00_共享/每日协调/G2_START_RELEASE_20260828.md`，本单转为非阻塞跟踪，不再拖延执行者 B 启动。
3. 仍须在最终锁数前完成平均值、最大值或 CPI 同比与源表对数；若计算失败，再由 A 修改底表类型并确认保存生效。

## 分层持久化核验更新（2026-09-04）

1. 重新加载指定正式模型后，`V50_country_monthly_risk.cpi_index` 仍显示长浮点型，本批没有改型、保存或发布正式模型。
2. 对本机已有9月1日19:46:31模型导出分层解析：`define.views[].fields[]` 中CPI是STRING，而同一视图/引用字段对应的 `AUGMENTED_DATASET/fields/field` 为BIGDECIMAL。因此不能仅读取底表视图层，就认定模型覆盖类型没有进入XML。
3. 原始XML SHA-256为 `fa99dddab3ab7146104a77ac5af06936c317953f93c253643378e10a5bd8314f`。这是已有受控备份，不是9月4日新导出。BIGDECIMAL属于长浮点数值类型，不应只接受字符串字面值DOUBLE。
4. 旧台账59项异常的模型字段类型均已在该备份持久化，且与9月4日在线类型逐项相符。历史底表审计原判定保留，备注补充分层结论；没有把59项全部改签验收通过。
5. 源端CPI基准：7680行、7616非空、64空值、均值375.7954180183989487788865546、最大值47954.2419061244。仅作为下一次平台对账预期值。
6. 本次临时即席、透视、仪表盘入口字段树均不可选，尚未取得平台全表计算结果。原因未定位，不归因于CPI类型或账号权限。临时页面未保存并已关闭。
7. 本单继续保持OPEN / NON-BLOCKING。剩余重点为平台实际计算对账与最终包一致性/恢复复核；不要重复要求无依据的底表改型。

证据：`A_数据平台/03_Smartbi证据/A03/A03_TYPE_PERSISTENCE_20260904/README.md`、`TYPE_LAYER_RECONCILIATION.json`、`CPI_SOURCE_BASELINE.json`。未代签B。

## 实际聚合复测更新（2026-09-04 15:35）

1. 新建临时原生指标卡绑定指定正式模型，通过“复制转度量”实测CPI：SUM=2,862,057.90、AVG=375.80，均与源端在两位小数显示精度一致；MAX=993.56，而源端数值最大值为47,954.24。本单现已取得实际计算失败证据，不再仅是缺证。
2. 在同一平台正式底表执行只读对照：7680行、7616非空、64空值，`MAX(cpi_index)=993.558523173606`；`MAX(CAST(NULLIF(TRIM(cpi_index),'') AS DECIMAL(30,12)))=47,954.24`，数值AVG=375.80。
3. 源文件文本序最大值恰为993.558523173606（IRN，2021-06-30），数值最大值为47954.2419061244（SDN，2023-02-28）。结合底层STRING，可确认本聚合路径仍按文本比较取极值。模型覆盖类型持久化没有消除此运行问题。
4. 先前“字段不可选”不能归因于账号权限；本次实际勾选已成功。类型台账仅F88追加运行失败及证据说明，FAIL和B签署均保持。没有修改只读源文件、正式源表或正式模型，也没有保存临时核验查询/仪表盘为正式资源。
5. 下一步按8月28日裁决执行底层数值化修复：先备份及核对引用，保存生效后复测原生AVG/MAX、7680/7616/64边界及受影响趋势/同比，再交B复核。只读CAST对照正确不等于正式修复完成。

状态：`OPEN / FAIL_MAX_PENDING_REPAIR`。对G2启动继续非阻塞，但阻止CPI最终指标验收及相关最终冻结。G4—G6不得因此签PASS。

证据：`A_数据平台/03_Smartbi证据/A03/A03_CPI_RUNTIME_RECHECK_20260904/README.md`。
