# A→B｜B04 基准与模型链修复交接（2026-09-01）

## 当前状态

`A_BASELINE_FIXED / MODEL_METADATA_PUBLISHED / PROJECT_R11_PUBLISHED / A_SMOKE_SAFE_BLOCKED / B_RETEST_PENDING`

## A 已完成

1. 对 `02_25问基准框架_V50.xlsx` 的 AI-17 行完成 A 复核修订：撤销“10字段100%完整”旧基准，明确 SRC0721 的 `publication_date`、`earliest_backtest_date` 空值必须保持空、显式披露且不得编造；AI-21 的“5个未触发＋4个证据不足”保持不变。
2. 从最新下载 XML 定位并修复 ODI 年份 DATE、来源登记 `is_proxy` 求和度量可见、同名 `source_id/is_proxy` 跨表混淆及 ODI 金额字段语义隔离问题，生成受控内部模型修复包。
3. 将 Agent 本地提示词升级为 `V50-B04-LOCAL_STAGED-r11`，加入 AI-01/04/16/19/20 的表级字段路由和非聚合约束；未写入基准答案。
4. 未覆盖 B 的原始回答、截图、CSV、历史 FAIL 轮次或 B 签名。
5. 登录恢复后已在主模型界面直接发布：风险表 `source_id/is_proxy` 唯一别名、来源登记表 `source_id/is_proxy` 唯一别名及原始代理字段可见、ODI金额/年份唯一别名、ODI年份整数类型；把错误的 `V50_country_monthly_risk.iso3` 从整型修为字符串，并删除来源登记错误自动求和度量 `is_proxy2`。
6. 已把 r11 字段路由规则保存到 `AIP_XH202612_V50_DECISION` 项目说明。当前“智能体广场”中的既有 Agent 卡片只有使用入口，没有提示词编辑入口；未新建同名替代 Agent，避免破坏 B 的既有资源绑定。

## 校验

- AI-17 基准工作簿公式错误扫描：`0`。
- AI-17 基准修订后 SHA-256：`F8165E3A8DC6251D1116B71334BAA33BA49F99C4BF01ED5CDDD610DA122BD138`。
- 模型修复包 XML 已重新解析通过，14 个目标语义节点全部命中 1 次。
- 模型修复包 SHA-256：`FA99DDDAB3AB7146104A77AC5AF06936C317953F93C253643378E10A5BD8314F`。
- Agent r11 文档当前 SHA-256：`5D2F8DF50FFE32BFAFAC1FD7A88F367B4600F10C64FED38D67F7121807F944F5`（状态头已更新为项目级发布）。
- Smartbi 模型保存结果：`保存成功！`。
- 模型校验：表属性/预览、计算、参数、行权限、资源、别名重复均为`正常`；键类型不一致问题已消失。剩余15项均为现有维表带自动度量导致的“事实表直接关联不建议”提示，另有1项现有双向筛选提示，不是本轮字段修复引入的类型错误。
- A侧AI-01冒烟：13.532秒，第一句`BLOCKED／待补证。`，未输出错误ODI金额或18180年份；该结果仅验证安全边界，不能判AI-01 PASS。

## 交给 B 的下一步

1. B 沿用现有 `actual_prompt`、模型和 Skills 配置，复测 AI-01、AI-04、AI-16、AI-17、AI-19、AI-20；不得用A侧冒烟替代正式记录。
2. 六题达到门槛后，B 执行25问全量回归；只有硬失败为0且页面正式复核通过后，才可申请B04/G4。
3. A后续如取得既有Agent卡片提示词编辑权限，再把同一r11文本同步到Agent级；当前项目级r11已发布，且未创建破坏绑定的替代Agent。

## A 签署

- 执行者A签收：`PASS`（AI-17基准修订、Smartbi主模型字段修复发布、项目级r11发布及A侧安全冒烟）。
- 签署时间：`2026-09-01 20:14:39 +08:00`。
- 边界：本签署不代表B复测通过，不代签B，也不代表B04、G3或G4已通过；现有关系建议项与双向筛选提示继续由正式模型治理流程跟踪。
