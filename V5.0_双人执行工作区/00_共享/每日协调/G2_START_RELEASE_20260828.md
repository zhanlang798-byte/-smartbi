# G2 启动放行裁决｜2026-08-28

- 生效时间：2026-08-28 11:43 +08:00
- 裁决状态：`G2 = PASS`
- 裁决性质：启动放行；允许执行者 B 正式开始 DB03、DB05、DB06 页面及 AIChat 配置。
- 历史边界：2026-08-27 的 `BLOCKED` 复核记录保留原样；本文件自生效时间起覆盖其“不得启动”结论，不倒改历史证据。

## 一、放行依据

1. 21 张对象行数证据在位；18 张正式表合计 313,593 行，三张辅助表为 40/20/660。
2. B00 自动核对为 23 PASS / 0 FAIL / 0 BLOCKED；共享模型字段树 27/27 可见。
3. Smartbi 正式模型 `MDL_XH202612_V50_COUNTRY_RESERVE` 可查询；实机显示 `cpi_index = BIGDECIMAL`，数据预览能正常返回数值。
4. 现有缺口已能被书面隔离，不需要继续阻塞 B 的页面和 AIChat 启动节奏。

## 二、PASS 后的执行边界

1. **DB03、DB06：**允许立即正式搭建并按现有参数清单逐组件对账。
2. **DB05：**允许按当前正式模型和页面执行稿直接搭建、逐组件对账并完成验收；根据 A 的最新确认，不再把年份关系 #14 的处理或复核列为 B 的待办和启动限制。
3. **CPI 指标：**允许使用正式模型中的处理后字段开展搭建；平均值、最大值或同比仍须与源表完成一次对数，完成前不锁定 CPI 最终指标。
4. **AIChat：**允许开始正式对象配置和 5 题冒烟；25 问仍须真实逐题执行，不能把离线基准写成实测 PASS。
5. A 后续如修改模型、关系或字段，必须记录变更版本和受影响对象，B 对受影响页面定向复测。

## 三、转为非阻塞跟踪的旧项

- `cpi_index` 底表/XML 为 `STRING`、模型运行时为 `BIGDECIMAL` 的元数据不一致。
- 平台全表主键查询、资源锁/同名处置证据不足。
- `V50_` 前缀复现入口与旧脚本退役记录。
- 交接材料版本/状态不同步。
- 孤立表涉及的跨表筛选联动核验。

以上项目不再阻塞 G2 和 B 启动，但仍影响相关最终指标验收、恢复演练或 G6 最终放行；不得删除问题单或伪写为已完成。

## 四、状态映射

- `G2 / B00：PASS（启动放行）`
- `G3：未开始（已获正式启动资格）`
- `G4：允许配置，正式实测仍为 0/25`
- `G5 / G6：未通过`

## 五、签收

- 执行者 B：✅（2026-08-28 11:43 +08:00，确认 G2 启动放行并接受上述书面边界）
- 执行者 A 回签：✅（2026-08-29 10:30 +08:00，本人确认 G2 启动放行并接受上述书面边界）
