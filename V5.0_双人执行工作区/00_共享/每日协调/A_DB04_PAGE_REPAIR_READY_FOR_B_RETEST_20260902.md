# A→B｜DB04 页面修复完成并提交复测｜2026-09-02

## 当前状态

`A_PAGE_REPAIR_DONE / READY_FOR_B_RETEST / G3_BLOCKED_UNTIL_B_PASS`

## A 已完成

1. 将旧标题 `筛选_1、筛选_3、筛选_4、筛选_5` 改为 `期限、策略、窗口截止日、周期状态`；国家与黄金权重标题保持业务化显示。
2. 在页面显著位置增加默认口径：未选择国家、期限、策略、窗口或周期时显示全量情景聚合，不代表单一情景。
3. 扩宽“0%—20%黄金网格”，使 `max_drawdown、recovery_months` 等关键列完整可读。
4. 将“反例与weight_sum检查”改为页面下方全宽独立表，保留 `scenario_id、strategy_id、iso3、horizon、window_end、gold_weight、weight_sum` 七个复核字段。
5. 查明原始 `gold_weight` 在 Smartbi 入模时被整型化，原始预览已全部为 `0`；仅修改显示格式无法恢复小数。A 未伪造该列，改以仍完整保留权重档位的 `scenario_id` 作为“黄金权重”筛选键，并仅开放七个正式网格值：
   - `grid_g0.000`
   - `grid_g0.025`
   - `grid_g0.050`
   - `grid_g0.075`
   - `grid_g0.100`
   - `grid_g0.150`
   - `grid_g0.200`
6. 页面已保存；选择 `grid_g0.025` 后，黄金网格、KPI、同键比较和反例表同步收敛到该档，恢复 `(全部)` 后回到默认全量聚合态。

## 证据

| 文件 | SHA-256 | 说明 |
|---|---|---|
| `01_DB04_默认全量预览_标题列宽已修.png` | `B32AB28E2986145656505D22884420B14DB66EA31ABC4735BF3EFB1AAE707B6F` | 正式预览、六个正式标题、扩宽网格和默认态 |
| `02_DB04_黄金权重七档候选.png` | `7CA4FAD45113D7318B46F153FCD0C3EA4E39416F2C7C545EF54AB86AFD98F930` | 七个 `grid_g0.xxx` 候选完整可见 |
| `03_DB04_权重档位2.5pct联动结果.png` | `6A7C2B740EADCBC7178801523FD221A53DA6083642A55572985D5B7A8D4C9B88` | 选择 `grid_g0.025` 后组件联动 |

证据目录：`../../A_数据平台/03_Smartbi证据/DB04/A修复复核_20260902/`

## 交给 B 的复测

1. 打开正式预览，核对六个筛选器标题与默认全量聚合说明。
2. 展开黄金权重筛选，核对七个精确 `grid_g0.xxx` 档位；分别抽测 `0.000、0.025、0.200`。
3. 核对每个档位下黄金网格、五项 KPI、三策略同键比较和反例表联动，并复核 `weight_sum=1`。
4. 独立记录完整响应时间并给出 `PASS/BLOCKED`；若不接受 `scenario_id` 受控映射，应明确要求重建并重新导入源表，而不是把已截断的 `gold_weight=0` 当作真实权重。

## A 签署

- 执行者A：`READY_FOR_B_RETEST`。
- 电子签署：`✅（A 已明确授权电子落盘）`。
- 时间：`2026-09-02 00:24:21 +08:00`。
- 边界：A 不代签 B；DB04 和 G3 在 B 独立复测签 `PASS` 前保持 `BLOCKED`。
