# B 对 A03 全量导入的只读复核记录（G2 前置）

- 复核时间：2026-08-25 21:26 +08:00
- 复核角色：执行者 B（AI 协助只读核对；B 本人已确认）
- 复核对象：`A_数据平台/03_Smartbi证据/A03/`
- 对象所有者：执行者 A；本次未修改 A 区任何文件
- 结论：**BLOCKED（A 报告已完成导入，但当前证据不足以让 B 独立签收）**

## 一、可以独立确认的内容

1. A00 锁定证据显示 18 张正式表均为 `data` 工作表，逐表行数、列数、主键重复/空值和 SHA-256 全部 PASS；合计 313,593 行。
2. A03 对账表已列出 18 张正式表和 3 张辅助表，目标对象统一使用 `V50_` 前缀。
3. 三张辅助表源端行数为 40/20/660；B 已在 A01/A02 复核中独立核对。
4. `MVP_company_data_status.xlsx` 与替代上传用 CSV 均为 20 行 × 17 列；CSV 原始字节带 UTF-8 BOM，去除 BOM 后列名一致、逐单元格差异 0。
5. A 已记录两项过程变更：平台 30 字符限制导致前缀由 `TB_XH202612_V50_` 改为 `V50_`；`MVP_company_data_status.xlsx` 被平台拒导后改用同内容 CSV。
6. A04 后续模型截图能够证明部分 `V50_` 对象已进入共享模型，但不能替代 A03 的逐表导入行数、主键和类型证据。

## 二、目前不能独立确认的内容

1. A03 目录只有 `01_import.png` 一张 220×231 平台截图，画面仅显示 6 张 `dim_*` 维表名称且没有显示 `V50_` 前缀；未覆盖 18+3 全部对象、逐表行数、主键读数和系统时间。
2. `SMARTBI_IMPORT_RECONCILIATION_V50.xlsx` 中正式 18 表的平台行数、重复和空主键均写成“`A报告`”，不是可复核的平台数值；判定也明确写着“截图由 A 存档待 B 抽查”。
3. `SMARTBI_RESOURCE_LOCK_V50.xlsx` 的“平台是否已存在同名”21 项全部为空，无法证明上传前无同名覆盖。
4. `SMARTBI_TYPE_AUDIT_V50.xlsx` 共 442 个字段，“平台类型”和“判定”列全部为空；`month_end`、`fx_reserves_usd`、`gold_price_usd`、`yield_spread_10y_3m`、`equity_drawdown` 等高风险字段未形成平台证据。
5. 三张辅助表虽有平台行数，但主键重复/空值的平台栏未填写。
6. `a03_prepare.py` 仍硬编码旧前缀 `TB_XH202612_V50_`，当前 `V50_` 对账表和资源锁无法由该脚本直接复现；命名变更虽有记录，但批准状态尚未书面闭环。
7. 当前证据只能支持“执行者 A 报告 A03 已完成”，不能支持“执行者 B 已独立复核 PASS”。

## 三、最小补证要求

1. A 提供 18+3 对象的可读对象清单与平台行数证据；可用逐表截图或一次可追溯的导出/查询结果，但必须显示对象名、真实行数和时间。
2. A 将对账表中的 21 项平台行数、主键重复、空主键改为真实数值，并填写对应证据路径。
3. A 补齐资源锁的同名检查结果，确认未覆盖旧对象。
4. A 提供平台字段类型导出或填写类型审计表；至少先闭环上述 5 个高风险字段、所有日期/主键字段，以及替代 CSV 的首字段名/类型（确认 BOM 未进入字段名）。
5. A 将准备脚本、对账表和资源锁的对象前缀统一到获批口径，或书面说明脚本不再作为复现入口。
6. 补证后由 B 再次只读抽查；全部满足后才能将本记录改为 PASS。

## 四、门槛影响

- A03 的执行状态可记为“**A 报告 PASS / B 复核 BLOCKED**”。
- G2 不升级；B00 不签收；B01/B02 不正式搭页。
- 本记录不否定 A 已经执行上传，只判定现有证据尚不足以完成双人交叉复核。

## 五、账号与本人复核说明

- A/B 使用项目同一 Smartbi 账号，这是既定运行前提；B 不修改 A 对象属于职责纪律，不以平台独立账号或技术不可写权限证明为验收项。
- 本记录由 AI 协助完成只读核对，执行者 B 已审核并确认结论无误；A03 仍因平台证据不足保持 BLOCKED。

签收：B ✅（2026-08-25 23:19 +08:00，本人确认）　A ✅（2026-09-01 20:23:14 +08:00，本人确认本文件记录的2026-08-25历史BLOCKED边界；后续启动放行以`G2_START_RELEASE_20260828.md`为准，不倒改历史结论）
