# B 复核 A03/A04｜2026-08-27

- 复核角色：执行者 B（AI 协助只读核对；本人已确认当前复核记录）
- 复核批次：`e0de1a5 A04/A05: 同步模型合同与DB01进度证据`
- 边界：A 区只读，未修改 A 文件；本记录仅写入共享协调区。
- 结论：`A03 = BLOCKED`，`A04/G2 = BLOCKED`，暂不签 B00，不正式启动 DB03/DB05/DB06。

## 一、已确认通过的范围

1. 21 张“预览数据”截图均在位；18 张正式表逐表行数合计 313,593，三张辅助表为 40/20/660。
2. B00 自动核对已对 A 最新模型包重跑：23 PASS / 0 FAIL / 0 BLOCKED。
3. 同一脚本对当前共享模型包回归也是 23 PASS / 0 FAIL / 0 BLOCKED；材料齐全性和本地边界读数未退化。
4. 关系审计表记录 19 条关系均为 PASS；指标模型按既有主办方范围豁免不适用。

> 上述 PASS 只覆盖材料、行数、离线边界和已有记录，不等于 A03、A04、G2 或 B00 最终 PASS。

## 二、未闭环项

| 复核项 | 结论 | 证据与原因 |
|---|---|---|
| 21 个对象的平台行数 | PASS | `SMARTBI_IMPORT_RECONCILIATION_V50.xlsx` 逐项挂接截图；18 表合计 313,593，辅助表 40/20/660。 |
| 平台主键重复/空主键 | BLOCKED | 对账表 21/21 仍写“源端审计+A报告；平台未查询”，不能代替平台查询证据。 |
| 上传前同名检查/资源锁 | BLOCKED | `SMARTBI_RESOURCE_LOCK_V50.xlsx` 21/21 均为“无法追溯”，未证明没有静默覆盖旧对象。 |
| 442 字段类型 | FAIL / BLOCKED | 当前 `SMARTBI_TYPE_AUDIT_V50.xlsx` 实际为 PASS 379、FAIL 63；日志/问题单又写放行 4 个布尔字段后应为 PASS 383、FAIL 59，但该修订未落回工作簿。`cpi_index` 页面显示长浮点型，而最新 XML 仍为 STRING。 |
| 前缀与复现入口 | BLOCKED | 平台对象使用 `V50_`，但 `A03/a03_prepare.py` 仍硬编码 `TB_XH202612_V50_`；缺有效复现入口或旧脚本退役书面记录。 |
| A04 交接包一致性 | BLOCKED | 交接书/变更日志称 19 条关系已建立，但 A 最新 `RELATIONSHIP_MAP_V50.xlsx` 仍是 19 条“待平台建”；共享关系图的总览/动作也仍保留旧的“拟建/平台未验证”描述。 |
| 年度政策关系 #14 | 待澄清 | 审计表为建前 2,744、建后 2,704、未匹配 40，却判 PASS；与交接书“建后行数=建前行数”及建模顺序“建前=建后”文字不一致。需明确这是已批准的 unmatched 例外，或修正审计口径/判定。 |
| 冻结与签收 | BLOCKED | 冻结版本、精确时间、A/B 签字仍为空。 |

## 三、B 侧复核意见

1. A03 的正式状态继续按 `BLOCKED` 管理；A 当前进度里的“人工检查通过”只能作为 A 自述，不能替代门槛结论。
2. `ISS-20260825-005` 与 `ISS-20260826-005` 继续保持 `OPEN / BLOCKED`。
3. A04/G2 不升级；B 不在未冻结模型上正式搭页，不替 A 修改模型、脚本或证据表。
4. A 补证后，B 重新执行自动核对、字段类型计数、关系口径核对和人工签收；不得只沿用本次 23/23 自动核对结果。

## 四、A 下一次最小交付包

1. 21 项平台主键重复/空主键查询证据，以及可回指对象和时间的索引。
2. 21 项资源所有者/创建时间/同名处置证据，或不可追溯时的正式风险接受记录。
3. 字段类型持久化问题的管理员结论或可复核替代导出；同时把 379/63 与 383/59 的版本差异落到同一份权威工作簿。
4. `V50_` 前缀批准记录和有效复现入口；旧 `a03_prepare.py` 若退役须书面标记。
5. 刷新 A04 交接包中的关系状态，澄清关系 #14 的 40 行 unmatched 口径。
6. 前述项目通过后再填写冻结版本/时间，并交 A/B 双方本人签收。

## 五、SmartBI 实机只读补充核验

1. 正式底表 `V50_country_monthly_risk`（节点 `TAB.input.input.null.v50_country_monthly_risk`）资源属性显示创建者 `18862589711`，创建时间 `2026-08-25 10:51:00`，最后修改 `2026-08-25 10:51:01`；底表结构中的 `cpi_index` 为 `STRING`。
2. 正式模型 `MDL_XH202612_V50_COUNTRY_RESERVE`（名称 `RELATIONSHIP_AUDIT_V50`，节点 `6b5dff57a4093ba3db07d2903905fe40`）显示创建者 `18862589711`，创建时间 `2026-08-25 11:00:37`，最后修改 `2026-08-27 01:31:01`；模型字段属性中的 `cpi_index` 为“长浮点型”。因此类型持久化不一致已由实机再次确认，不能按 PASS 处理。
3. 模型画布实机读取为 22 张表、19 条关系；其中 6 张孤立表为 `V50_monthly_risk`、`V50_case_evidence`、`V50_historical_crisis_event`、`V50_source_registry`、`V50_dim_event`、`V50_dim_asset`。
4. 关系 #14 实机配置为 `V50_dim_year.year_key = V50_country_policy_year.year`，基数“一对多”，筛选方向“单向”。结构配置与合同一致，但本地审计的 2,744→2,704、未匹配 40 行口径仍未获批准或修订，故该项继续待澄清。
5. 底表预览实际仅返回 50 行；按 `iso3 + month_end` 对这 50 行抽样检查，空键 0、重复键组 0、`cpi_index` 非数值 0。该结果仅为页面样本，不能替代全表主键空值/重复查询证据，A03 继续 BLOCKED。
6. 全程未保存、未修改 A 对象；关闭模型时出现“模型数据结构发生改变，是否保存？”提示，已明确选择“不保存”。

## 六、签收前 A / B / 共享区总复核

1. Git 当前 `HEAD=e0de1a5`，与 `origin/main` 一致；暂存区为空，A 区没有未提交改动。
2. A 区关键证据再次核对：21/21 对账项仍缺平台全表主键查询；21/21 资源锁仍为“无法追溯”；类型审计实算 379 PASS / 63 FAIL；关系审计 19 PASS，但关系 #14 仍为 2,744→2,704、未匹配 40。A 的 `当前进度.md` 仍引用 8 月 25 日门槛表，并把 B 只读验证写为待闭环，须由 A 自行刷新，B 不代改。
3. B 区核验脚本再次执行成功：23 PASS / 0 FAIL / 0 BLOCKED，脚本语法检查通过；DB03/DB06 离线参数已完成，正式页面仍未启动。
4. 共享区晚报、门槛表、对象锁和复核记录状态一致，均保持 `G2/B00=BLOCKED`，未发现把自动核对 PASS 冒充最终门槛 PASS 的记录。
5. A 模型证据目录与共享模型交接目录的 8 件同名材料中，`RELATIONSHIP_AUDIT_V50.xlsx`、`METRIC_DICTIONARY_V50.xlsx` 哈希一致；其余 6 件因精简版/增强版布局或状态更新不同步而哈希不同。冻结前须由 A 刷新为单一权威版本。
6. 综上，可以签收“本复核记录及当前 BLOCKED 边界”；不能签收 G2/B00 最终 PASS。

## 七、签字

- 执行者 B：✅（2026-08-27 22:50 +08:00，本人确认本复核记录及当前 BLOCKED 边界；不代表 G2/B00 通过）
- 执行者 A 回签：✅（2026-08-29 20:57:03 +08:00，本人确认本文件记录的2026-08-27历史BLOCKED边界；不倒改历史结论。2026-08-28后的启动放行以`G2_START_RELEASE_20260828.md`为准）
