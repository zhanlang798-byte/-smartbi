# B → A：V50 Agent 正式复测 BLOCKED／退回重新处理（2026-09-02）

> 落盘时间：2026-09-02 13:09 +08:00
>
> 当前状态：`B_RETEST_BLOCKED / A_RECHECK_REQUIRED / G4_BLOCKED`
>
> B 判定：AI-17 PASS；AI-04、AI-16 FAIL；AI-01 尚不足以签正式 PASS；AI-19、AI-20 暂停。

## 1. 本次退回结论

B 已按原始 B05 计划书规定，在 `AGENT_XH202612_V50_ASSISTANT` 中使用冻结问句复测。AI-04、AI-16 首答仍因知识库没有月度明细而返回 `BLOCKED／待补证`，未完成正式模型问数。

A 于 2026-09-02 提交的自测证据中，多张 AI-01 截图显示输入资源标签为“第二次”，没有显示正式冻结资源 `MDL_XH202612_V50_COUNTRY_RESERVE`；AI-04 FINAL 图还显示了与 AI-04 问句不对应的黄金、白银类模型字段。因此，现有 A 自测证据不能证明在规定测试对象、规定冻结资源和规定冻结问句下已修复。

本次按“**疑似自测对象／资源不一致，退回 A 重新核验和处理**”落盘；不将“第二次”默认认定为正式冻结模型，也不删除 A 的历史证据。

## 2. 原计划书规定的正式测试对象

| 对象 | 正式标识 |
|---|---|
| QA 集 | `QA_XH202612_V50_AICHAT_25` |
| Agent | `AGENT_XH202612_V50_ASSISTANT` |
| 冻结模型 | `MDL_XH202612_V50_COUNTRY_RESERVE` |
| 知识库 | `KB_XH202612_V50_GOVERNANCE` |

判分必须使用冻结 `actual_prompt`，每题独立新对话，保留完整首答；不得用简化短问句、推荐问句或“重新生成”替代正式首答。

## 3. B 正式复测事实

| 题号 | 通道与资源 | 首答事实 | B 判定 |
|---|---|---|---|
| AI-17 | V50 Agent | 返回 `SRC0721` 唯一登记值，未登记日期保持空；耗时 7.926 秒 | PASS |
| AI-04 | V50 Agent + 正式 V50 MDL 标签 | 以“知识库未提供 TUR 月度明细”为由返回 `BLOCKED／待补证`；未返回三项冻结值；耗时 3.898 秒 | FAIL |
| AI-16 | V50 Agent + 正式 V50 MDL 标签 | 以知识库月度字段缺失为由返回 `BLOCKED／待补证`；未返回 12 个月明细及首末差；耗时 10.134 秒 | FAIL |
| AI-01 | V50 Agent | 命中 A 新增的 AI-01 专项知识并返回部分预期内容；现有截图尚未完整证明所有冻结字段、样本数和完整首答 | 暂不签 PASS |
| AI-19／20 | 未继续 | 当前链路对基础月度问数已经 BLOCKED，继续长报告题无有效验收意义 | 暂停 |

已有“数据洞察”结果仅保留为 MDL 映射诊断旁证，不替代 V50 Agent 正式判分。

## 4. 已核实的阻断证据

1. V50 Agent 当前编排可见链路仅为：`开始 → 知识库检索 → 大模型 → 结束`。
2. 编排页未显示数据模型查询、报表取数、SQL、数据问答等节点，`MCP设置` 亦未选择。
3. `AGENT_SYSTEM_PROMPT_V50.txt` 规定：知识库无证据时第一句必须为 `BLOCKED／待补证`，并要求只依据当前问题和本轮知识库检索结果。
4. 该配置与 AI-04、AI-16 的 B 复测首答一致：Agent 没有取得 `V50_country_monthly_risk` 明细，而不是冻结问句本身有误。
5. A 的 AI-01 自测图显示“第二次”资源；`04_AI04_TUR_LATEST_VALUES_PASS_FINAL_20260902.png` 展示内容与 AI-04 要求不匹配，需替换或说明。

## 5. 请 A 重新处理

### 5.1 先确认测试资源

1. 说明“第二次”的真实技术 ID、显示名来源、包含表及与 `MDL_XH202612_V50_COUNTRY_RESERVE` 的关系。
2. 如“第二次”不是正式冻结模型，原有以“第二次”为输入资源的证据不得用于正式判分。
3. 如“第二次”声称是正式冻结模型的别名或修订版，需提供可核验的资源详情页，证明技术 ID、版本和包含表完全对应；仅凭聊天框标签不接受。

### 5.2 按相同冻结条件复现并修复

1. 使用 `AGENT_XH202612_V50_ASSISTANT`、`MDL_XH202612_V50_COUNTRY_RESERVE`、`KB_XH202612_V50_GOVERNANCE`。
2. 逐字使用 `B04_六题复测执行单_20260902.md` 中 AI-04、AI-16 冻结问句，不缩短、不改写。
3. 先证明 Agent 能实际查询冻结 MDL，而不是继续把每道题的标准答案定点写入 KB。
4. 若根因在 MDL 字段、类型或来源映射，由 A 修复模型并回传差异；若根因在 B 所属 Agent／KB 编排，请 A 在回执中给出需要调整的节点、参数和影响范围，避免静默覆盖。
5. 修复后先由 A 完成 AI-04、AI-16 同条件首答自测，再提交 B 独立复测。

### 5.3 返还证据要求

每题证据必须同时看见：

- Agent 名 `AGENT_XH202612_V50_ASSISTANT`；
- 正式冻结模型标识 `MDL_XH202612_V50_COUNTRY_RESERVE`；
- 冻结问句全文；
- 完整首答；
- 实际耗时；
- AI-04 的逐指标原始值、日期、单一 `source_id` 和布尔元数据；
- AI-16 的最近 12 个月明细、首末差及月度原始元数据。

A 返还后，B 先复测 AI-04、AI-16；通过后再继续 AI-01、AI-19、AI-20，最后执行 AI-01—AI-25 全量回归。G4 在此之前保持 BLOCKED。

## 6. 证据索引

### B 本轮证据

目录：`B_AI_CHAT_V50_AGENT_RETEST_BLOCKED_20260902_证据/`

1. `01_B正确进入V50_Agent并挂载V50_MDL_20260902.png`
2. `02_Agent工作流仅KB检索_大模型_结束_无取数节点_20260902.png`
3. `03_AI17_SRC0721_KB追溯_PASS_20260902.png`
4. `04_AI04_V50Agent_KB无月度数据_BLOCKED_20260902.png`
5. `05_AI16_V50Agent_KB无月度数据_BLOCKED_20260902.png`
6. `06_AI16_V50Agent_字段全缺失_BLOCKED_20260902.png`

### A 原提交证据（保留原位，不删除）

目录：`../../A_数据平台/03_Smartbi证据/P1/A_AI_CHAT_REPAIR_20260902/`

重点复核：

- `03_AI01_INVESTMENT_FX_INTERSECTION_PASS_20260902.png`
- `08_AI01_INVESTMENT_FX_INTERSECTION_PASS_FINAL_20260902.png`
- `09_AI01_INVESTMENT_FX_INTERSECTION_TOP_PASS_20260902.png`
- `04_AI04_TUR_LATEST_VALUES_PASS_FINAL_20260902.png`
- `05_AGENT_PUBLISHED_CONFIG_20260902.png`

## 7. 落盘说明

- B 已明确要求将本轮复测事实同步至仓库并退回 A 重新处理。
- 本回执只记录已核实事实和复测边界，不修改 Agent、KB 或 MDL，不覆盖 A 历史证据。
- A 修复前，B 不再继续 AI-19、AI-20，避免产生无效正式证据。
